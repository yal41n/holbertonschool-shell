#!/usr/bin/env bash
# This script stops the 4-to-infinity-and-beyond process using pkill.

# pkill finds the process by its command line name and sends SIGTERM (signal 15)
# The -f flag ensures it matches against the full command line name.
pkill -f "4-to_infinity_and_beyond"
